server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pabloejover/finalBackend1.git
          default-label: main
          search-paths: configuration